---
import { cn } from "@/lib/utils";
import type { ClassValue } from "clsx";

interface Props {
	class?: ClassValue;
}
const { class: className } = Astro.props;
---

<h1 class={cn("font-semibold text-lg tracking-tight leading-none", className)}>
	<slot />
</h1>
