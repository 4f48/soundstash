---
import ResetPage from "@/components/reset-page";
import Layout from "@/layouts/html.astro";

const token = new URLSearchParams(Astro.url.searchParams).get("token") || "";
---

<Layout>
	<ResetPage token={token} client:idle />
</Layout>
