---
import HeaderComponent from "@/components/header";
import Layout from "@/layouts/html.astro";

const session = Astro.locals.session;
---

<Layout>
	<HeaderComponent authenticated={session} client:idle />
	<main class="mt-16">
		<slot />
	</main>
</Layout>
